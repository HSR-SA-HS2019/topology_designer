import React from 'react';

const AddNodeDialog = () => (
    <div id={AddNodeDialog}>
        <div>
            Choose a Device
        </div>

        <form>
            <input type="text" id="device"/>
        </form>
    </div>
);

export default AddNodeDialog;
